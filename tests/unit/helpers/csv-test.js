import { csv } from 'coop-dash/helpers/csv';
import { module, test } from 'qunit';

module('Unit | Helper | csv');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = csv([42]);
  assert.ok(result);
});
